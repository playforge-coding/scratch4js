// A helper module — proof that the extension can be split across files. This
// has nothing to do with Scratch; it's just normal code the bundle inlines.

/** @param {string} name */
export function greet(name) {
  return `Hello, ${name}!`;
}
